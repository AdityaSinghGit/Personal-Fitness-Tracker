// Central place for the Gemini model id. Change if Google updates stable ids.

class GeminiConfig {
  GeminiConfig._();
  // Widely available; if your key errors, try: gemini-1.5-flash-latest
  static const String modelName = 'gemini-1.5-flash-latest';
}
